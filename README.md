# unpackIso

Target:
Help user to check the iso file

Usage:
   $(basename $0) -i <input bootimage.iso> -
   chant debug isolinux.cfg
   
   output files:
        isolinux-check.cfg
		structure.log
		syslinux-check.cfg
   
ENDUSAGE
